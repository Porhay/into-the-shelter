import * as config from './config'

export const ROUTES = {
    /* Pages */
    MAIN: '/',
    WELCOME: '/welcome',
    ROOMS: '/rooms',
    SETTINGS: '/settings',
    PROFILE: '/profile',

    /* All other */
    GOOGLE_LOGIN: config.accountsUrl + '/api/auth/google/login'
}


